<Window x:Class="SD106_Onewhero_Assessment_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SD106_Onewhero_Assessment_2"
        xmlns:userControls="clr-namespace:SD106_Onewhero_Assessment_2.View.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown" Background="Transparent" AllowsTransparency="True">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="401*"/>
            <ColumnDefinition Width="399*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <userControls:MenuBar Grid.ColumnSpan="2"/>

        <!-- Content -->
        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="80*"/>
                <ColumnDefinition Width="10*"/>
            </Grid.ColumnDefinitions>

            <!-- Left spacer -->
            <Rectangle Fill="LightGray" Grid.Column="0"/>

            <!-- Center Content -->
            <Rectangle Fill="LightBlue" Grid.Column="1"/>
            <Frame  Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>

            <!-- Right spacer -->
            <Rectangle Fill="LightGray" Grid.Column="2"/>

        </Grid>

        <!-- Footer -->
        <Rectangle Grid.Row="1" Fill="Green" Grid.ColumnSpan="2" Margin="0,334,0,0" Grid.RowSpan="2"/>

        <!--<TextBlock Name="tbstatus" FontSize="50"/>
        <Button Name="btnToggleRun" Width="100" Height="50" VerticalAlignment="Center" HorizontalAlignment="Center"
                Content="Learn More" FontSize="15" Click="btnToggleRun_Click" />-->
    </Grid>
</Window>
